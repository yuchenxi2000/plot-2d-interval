# 更新日志 / Changelog

## [0.2.0] - 2026-08-25

### 新增

- 为跨越函数定义域边界的像素增加自适应子像素细分。
- 支持通过 `plotter.plot(depth)` 设置细分深度，默认值为 8。
- 增加 `ln(x)` 渐近线附近曲线完整性的回归测试。
- 在 `.gitattributes` 中将 PNG、JPG 和 JPEG 图片显式标记为二进制文件。

### 修复

- 修复 `ln(x)`、`1/x²` 等函数靠近渐近线的可见尾部缺失问题。

### 变更

- 更新受影响的示例图片。
- 将 README 重组为使用方法、效果展示和参数设置三个部分。
- 补充测试及绘图临时文件的忽略规则。

## [0.1.0] - 2026-01-14

### 初始版本

- 使用 C++ 和区间算法实现二维隐函数绘图。
- 提供示例函数、二进制绘图数据输出和 Matplotlib 图片生成脚本。
